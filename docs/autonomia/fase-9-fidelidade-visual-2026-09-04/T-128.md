# T-128 — Transparência da simulação UAT

## Contrato curto

- **Autorização:** Tossi, explícita, em 2026-09-08, após explicação do bloqueio P1 da T-127.
- **Meta verificável:** a faixa global `Modo simulação` continua visível em Conteúdo, Campanhas e Integrações quando a base for sintética.
- **Escopo de escrita:** `src/app/(app)/conteudo/page.tsx`, `src/app/(app)/conteudo/campanhas/page.tsx`, `src/app/(app)/integracoes/page.tsx`, seus testes visuais e os documentos desta missão.
- **Fora de escopo:** conteúdo dos indicadores, banco, migrations, produção, credenciais, integrações, lançamentos, login, permissões e qualquer dado real.
- **Aceite:** nenhuma das três telas contém regra que oculte `[data-faixa-simulacao]`; testes de regressão protegem as três; prévia UAT mostra o aviso; TypeScript e revisão independente aprovados.
- **Janela:** até 55 minutos, um executor, pulso em até 30 minutos; uma escalada para Terra somente por falha técnica persistente.
- **Desbloqueio:** retomar a T-127 apenas depois de a T-128 estar aprovada.

## Execução e evidências

- **TDD:** os três testes de regressão falharam antes da correção porque as páginas ainda continham a regra de ocultação; depois da remoção mínima, `npx vitest run` nos três arquivos concluiu com **16/16 testes aprovados**.
- **TypeScript:** `npx tsc --noEmit --incremental false` concluído sem diagnóstico.
- **Escopo e segredos:** `git diff --check` aprovado; o delta não contém segredo, credencial, chamada externa nem alteração fora das três páginas e respectivos testes.
- **Prévia UAT autenticada:** `/conteudo`, `/conteudo/campanhas` e `/integracoes` exibem a faixa global `Modo simulação` antes do conteúdo da página.
- **Revisão independente:** aprovada; confirmou a remoção exclusiva das três regras CSS, cobertura de regressão por tela e ausência de alteração de dados, banco, credenciais ou integrações.
