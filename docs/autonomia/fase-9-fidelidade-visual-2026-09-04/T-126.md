# T-126 — Financeiro e indicadores com fidelidade visual

## Contrato curto

- **Origem:** contrato-mestre da Fase 9, aprovado por Tossi.
- **Dependência:** T-125 recebeu a aprovação visual humana; sua revisão independente somente leitura segue em curso e não autoriza alteração de dados.
- **Escopo local:** `/financeiro` como visão operacional; acessos existentes a fluxo de caixa e análises. O caminho real de análises é `/analise`; a escrita de escopo do contrato-mestre (`analises`) é tratada como erro de pluralização, não como nova rota.
- **Proibido:** banco, migrations, produção, credenciais, integrações, cobranças, lançamentos financeiros ou criação de despesa. Os controles desta tarefa permanecem visuais/locais até uma autorização específica de comportamento.
- **Aceite:** referência visual aprovada antes de código; métricas estritamente derivadas da base ou indisponibilidade explícita; financeiro secundário à jornada de mentoria; testes focados, TypeScript, prévia autenticada, revisão e varredura de segredos.

## Referência proposta

- Arquivo: `referencia-financeiro-proposta.png`.
- SHA-256: `BFE202115BAAF932507E865C1611958DBBB9E2F9C866346B06BCF7B08A684A8B`.
- Composição: sustentabilidade da operação, quatro métricas compactas, orçamento e prioridades, leitura por programa, contexto de continuidade dos atendimentos e acesso seguro a análises.
- Limite: todos os valores, barras e percentuais do modelo são somente composição. A implementação usará o estado já calculado ou declarará ausência.
- **Aprovação:** Tossi, explicitamente, em 2026-09-07.
- **Próxima célula:** TDD e implementação local de `/financeiro`, preservando os gráficos, tabelas, formulários e rotas existentes abaixo da primeira dobra.

## Evidência de implementação

- O resumo aprovado foi inserido antes dos dados completos em `src/app/(app)/financeiro/page.tsx`, com o marcador `data-financeiro-visual="referencia-aprovada"`.
- Os quatro indicadores usam os totais já calculados para o ano selecionado; orçamento, programas, prioridades e saúde exibem ausência explicitamente quando a base não permite leitura.
- Destinos preservados: `/analise`, `/financeiro/caixa` e o formulário existente `#registrar-despesa`.
- TDD: `npx.cmd vitest run "src/app/(app)/financeiro/page.test.ts" "src/app/(app)/financeiro/page.visual.test.ts" --reporter=dot` — 2 arquivos, 5 testes aprovados.
- TypeScript: `npx tsc --noEmit` — aprovado.
- Prévia autenticada local: `/financeiro`, em 2026-09-07, confirmou a composição e o estado honesto de base vazia. A revisão independente e a publicação ainda são a célula seguinte.

## Revisão independente

- A primeira revisão encontrou um desvio P1 de hierarquia: `Leitura de operação` estava abaixo de `Prioridades`, e `Próximo passo` ocupava a coluna direita.
- A correção colocou `Visão por programa` e `Leitura de operação` lado a lado e transformou `Próximo passo` em faixa inteira abaixo, como na referência aprovada.
- O teste visual passou a proteger a ordem `Prioridades → Visão por programa → Leitura de operação → Próximo passo`.
- Rechecagem independente: **APROVADO**; preservação de aviso de simulação, rotas/legado e ausência de segredo confirmadas. A publicação é a próxima célula.
