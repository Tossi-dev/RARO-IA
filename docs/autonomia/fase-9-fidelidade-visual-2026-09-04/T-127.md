# T-127 — Gate final de fidelidade visual e regressão

## Contrato curto

- **Origem:** Fase 9 aprovada por Tossi; T-121 a T-126 concluídas com referências e validações humanas registradas.
- **Gatilho:** Tossi confirmou a validação visual humana da T-126 em 2026-09-07.
- **Meta verificável:** confirmar a matriz completa de referências aprovadas, screenshots finais e regressões técnicas sem alterar a rota `/`.
- **Escopo de escrita:** somente esta pasta documental e o ledger/`Onde parei.md` da missão.
- **Proibido:** mudança de interface, banco, migrations, produção, credenciais, integração externa, dados reais ou lançamento financeiro. Uma divergência visual vira evidência e bloqueio localizado; não autoriza correção fora do contrato.
- **Recursos compartilhados:** repositório RARO IA, porta local 3000 e lock lógico da Fase 9.
- **Janela:** até 55 minutos; pulso em até 30 minutos. Executor único; uma única escalada para Terra somente diante de falha técnica persistente.
- **Aceite:** referências aprovadas e prévias finais rastreáveis; suíte pertinente, TypeScript e build aprovados; rota `/` sem regressão; inventário de diff e segredos limpo; revisão independente aprovada.

## Matriz de validação

| Grupo | Rotas a conferir | Referência aprovada |
| --- | --- | --- |
| Shell e visão | `/painel` | `referencia-painel-aprovada.png` |
| Mentoria e agenda | `/mentoria`, `/agenda` | `referencia-mentoria-aprovada.png`, `referencia-agenda-aprovada.png` |
| CRM, jornadas e portal | `/crm`, `/trilhas`, `/portal` | `referencia-crm-aprovada.png`, `referencia-trilhas-aprovada.png`, `referencia-portal-aprovada.png` |
| Conteúdo e operação | `/conteudo`, `/conteudo/campanhas`, `/integracoes`, `/comecar` | referências aprovadas correspondentes |
| Financeiro | `/financeiro` | `referencia-financeiro-proposta.png` |

## Evidências a registrar

- Navegação autenticada pelas rotas acessíveis ao papel atual, com estados vazios tratados como ausência honesta.
- Testes de fidelidade visual existentes e TypeScript/build.
- Caso adversarial da rota `/` e conferência responsiva de uma tela representativa.
- `git diff --check`, inventário final e varredura de segredos.
- Revisão independente somente leitura.

## Checkpoint de validação

- Testes diretamente relacionados à Fase 9: **79 testes aprovados em 13 arquivos**. As mensagens de `form action` vêm da renderização de Server Actions em teste e não reprovaram casos.
- TypeScript concluiu sem diagnóstico; o `next build` concluiu e gerou `.next/BUILD_ID` em 2026-09-07 21:52 local.
- Caso adversarial de `/`: sem sessão, `GET /` respondeu `307 → /login`; no navegador autenticado em modo UAT, `/` abriu o launcher e mostrou a faixa global de simulação. Não há delta em `src/app/page.tsx` nem `src/app/(app)/inicio/**` no intervalo da Fase 9.
- A suíte completa foi iniciada, mas o processo do Vitest ficou sem atividade após a parte já verde e foi pausado após dois pulsos sem delta, conforme o contrato. Ela não é contabilizada como aprovação completa.

## Bloqueio de liberação

1. **P1 — transparência UAT:** `src/app/(app)/conteudo/page.tsx`, `src/app/(app)/conteudo/campanhas/page.tsx` e `src/app/(app)/integracoes/page.tsx` escondem `[data-faixa-simulacao]` por CSS. Isso repete o problema corrigido na T-125 e viola o contrato de `src/components/simulacao.tsx`: dado fictício não pode aparecer silenciosamente.
2. **P2 — responsividade final:** a captura responsiva não foi executada porque o P1 anterior já impede o gate de liberação.

**Estado:** T-127 está pausada e a Fase 9 não recebe selo `ready_for_production`. A próxima ação segura é uma tarefa corretiva limitada à remoção dessas três regras de ocultação, com testes de regressão e nova validação visual UAT.
