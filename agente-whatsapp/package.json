{
  "name": "raro-agente-whatsapp",
  "version": "1.0.0",
  "private": true,
  "description": "Agente local que liga o WhatsApp do dono ao Raro.ia. Roda no MacBook, so faz conexao de saida.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.26.0"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
